# NOTE: Developer helper script.
# For CRAN, do not run installation or package-building commands from within package code or examples.

# Load the tools
library(devtools)

# This will now work because it can read the plain text DESCRIPTION file
# devtools::document()



# This installs the package to your library
# devtools::install()
